<div class="card {{ card_classes }} {{ card_type }}">
  {% block card_image %}
    {% if card_image is not empty %}
      <div class="media">
        <img class="card__img" src="{{ card_image }}" alt="Alt Text">
      </div>
    {% endif %}
  {% endblock %}

  <div class="card__body">
    {% block heading %}
      {% if card_title is not empty %}
        {% include "@heading" with {
          "heading_level" : 'h2',
          "heading_class" : 'card__title',
          "heading" : card_title,
          "heading_url" : card_link_url,
          "heading_aria" : card_aria
        } %}
      {% endif %}
    {% endblock %}

    {% block card_author %}
      {% if card_author %}
        <div class="card__author">{{ card_author }}</div>
      {% endif %}
    {% endblock %}

<figure>
<div data-embed-button="media_entity_embed" data-entity-embed-display="view_mode:media.full" data-entity-type="media" data-entity-uuid="c20f2882-67b9-4000-907e-28dccd3aac52" data-langcode="en" class="embedded-entity">
	<div class="media media--type-image media--view-mode-full">


<img width="700" height="700" alt="Gold highlight shown behind emphasized display text used on a photo" loading="lazy" class=" lazyloaded" data-src="/sites/brand.uiowa.edu/files/2020-01/Elements-highlight%20over%20photo.png" src="https://brand.prod.drupal.uiowa.edu/sites/brand.uiowa.edu/files/2020-01/Elements-highlight%20over%20photo.png"></div>
<figcaption>researching new therapies for malignant peripheral nerve sheath tumors, which are deadly, resistant to drug therapies, and often unable to be operated upon due to their location.</figcaption>

</div>
</figure>

    {% block card_content %}
      {{ card_content }}
    {% endblock %}

    {% block card_bttn %}
      {% if (card_link_title is not empty) and (card_link_url is not empty) and (card_title is not empty) %}
        <div aria-hidden="true" class="bttn bttn--outline bttn--tertiary bttn--sans-serif"{% if card_aria %} id="{{ card_aria }}"{% endif %}>
          {{ card_link_title }}
          <i class="fas fa-arrow-right"></i>
        </div>
      {% elseif (card_link_url is not empty) and (card_title is empty) %}
        <a class="bttn bttn--outline bttn--tertiary bttn--sans-serif bttn--full" href="{{ card_link_url }}">{{ card_link_title }}
          <i class="fas fa-arrow-right"></i>
        </a>
      {% endif %}
    {% endblock %}
  </div>
</div>
