{# @todo Add check for no url to display <span> instead. #}
{% if link.url %}
<a href="{{ link.url }}">
  {% else %}
  <span>
{% endif %}
    {% if link.icon_class %}
  <i class="{{ link.icon_class }}"></i>
  <span class="element-invisible">
    {% endif %}
    {{ link.text }}
    {% if link.icon_class %}
  </span>
  {% endif %}
    {% if not link.url %}
</span>
  {% else %}
</a>
{% endif %}