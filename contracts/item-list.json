{
  "$schema": "./contract.schema.json",
  "name": "item-list",
  "version": "1.0.0",
  "description": "A prescribed COMPOSITION, not a component: a list of items — news, events, search results, \"related items\" — is a stack of BORDERLESS Cards, one per row, never hand-written markup. It owns no code and no tokens; every value it renders belongs to contracts/card.json, which is why tokensUsed is empty. What this contract adds is the set of Card options the arrangement PINS, the evidence behind each pin, and the counter-case the list is reliably confused with — a grid of BORDERED cards. Reference implementation, named by the rule: https://sandbox.prod.drupal.uiowa.edu/news — the SiteNow multisite site `sandbox.uiowa.edu` (identified by docroot/sites/sandbox.uiowa.edu/blt.yml, whose stage_file_proxy.origin IS that hostname), served by views.view.articles display page_articles (path: news). VERIFICATION BASIS, 2026-08-22: every markup claim below was re-derived from the EMITTING SOURCE in uiowa/uiowa @ 9b8cffee, not from the rendered page, which no session has fetched since these claims were first written. Source is the stronger evidence where it reaches, because it says WHY the markup has that shape and therefore whether the shape is guaranteed or incidental; it cannot settle content (which rows carry an image or a byline) and it cannot read the sandbox's live, admin-editable config. Each claim below now says which kind it is. THE ROW COUNT IS CONFIGURED, not coincidental: the default display sets pager type `full`, items_per_page 10 (views.view.articles.yml:56-58) and page_articles overrides only footer and sorts (`defaults: {footer: false, sorts: false}`), inheriting that pager — so page 1 is ten rows given ten published articles.",
  "identity": {
    "kind": "pattern",
    "storybookId": "components-card--item-list",
    "relatedPaths": [
      "src/components/grid/Grid.vue",
      "src/components/grid/GridItem.vue",
      "src/scss/layout/_grid.scss",
      "src/scss/abstracts/_grid-mixins.scss",
      "src/components/card/Card.stories.js"
    ],
    "downstreamPaths": [
      "uiowa/docroot/modules/custom/uiowa_core/src/Entity/NodeBundleBase.php",
      "uiowa/docroot/modules/custom/uiowa_core/uiowa_core.module",
      "uiowa/docroot/themes/custom/uids_base/templates/views/views-view.html.twig",
      "uiowa/docroot/themes/custom/uids_base/scss/components/grid.scss",
      "uiowa/docroot/themes/custom/uids_base/scss/global.scss"
    ],
    "figma": {
      "fileKey": "hNShklBztaeaQneScM0KoM",
      "page": "Components / Card",
      "note": "DOCUMENTATION FRAME 463:44, \"Item list — the arrangement, and the grid it is mistaken for\". Two sections drawn straight from children.card.fixed: the list (three Right/Borderless/Circle Card instances stacked, 32px row gap bound to space/200) and the grid counter-case (three Stacked/Bordered/Label across, same bound gap).",
      "precedent": "Card frame 365:44 (\"what the variant set cannot show\"), and the Foundations / Layout doc page that contracts/layout.json points at.",
      "measured": {
        "row-gap": "32, bound to Primitives space/200",
        "list-row": "720x213 per Card instance",
        "grid-tile": "480x524 per Card instance"
      },
      "deliberateOmissions": [
        "NOT a component set, and that is the point: a pattern owns no code and no tokens, so there are no variant axes to bind and nothing for check-figma to diff. The frame is snapshot-neutral — figma-readback.js captures COMPONENT_SET nodes only — so it adds no gate debt.",
        "The list rows show Card default 52.5% media basis, NOT the media_size: small this pattern pins (an absolute 250px wrapper / 218px image). Card has no media_size axis, so no instance can express it, and resize() on an instance sub-layer is a silent no-op. The frame caption says so rather than the frame faking it by detaching from Card."
      ]
    }
  },
  "composedOf": [
    "card"
  ],
  "options": [
    {
      "name": "arrangement",
      "type": "enum",
      "values": [
        "list",
        "grid"
      ],
      "default": "list",
      "description": "Which of the two card arrangements the content wants — the only decision this pattern asks an author to make, and the one the rule exists to settle. `list` is the pattern: a SEQUENCE of items where every row is the same kind of thing and the reader scans down. `grid` is the COUNTER-CASE, carried here as a sibling rather than left to prose because it is precisely what a list gets mistaken for: a small SET of parallel destinations, like a three-across \"related programs\" block, where the reader is choosing between siblings instead of scanning a feed. The two INVERT most of the pins, so children.card.fixed is keyed BY ARRANGEMENT: read `fixed.list` and `fixed.grid` for plain values and `fixed.notes` for the evidence behind each one. Rule of thumb with an actual edge: if the item count is open-ended, or the items come out of a view, it is a list.",
      "code": {
        "classes": {
          "list": "list-container list-container--list, emitted by Grid.vue:41-43 for type=\"list\" — the one branch that appends NO grid--* class > list-container__inner > grid__column per item (views-row downstream)",
          "grid": "list-container grid--threecol--33-34-33 > list-container__inner > grid__column per item"
        },
        "notes": "EACH ARRANGEMENT IS A CLASS TRIPLE ACROSS THREE DOM LEVELS, and the schema allows one string per enum value, so each value above is written `outer > inner > per-item`. None of the three levels does anything alone, which is the whole reason to record them together. UIDS emits the outer pair from Grid.vue, whose class is chosen by a two-branch computed (Grid.vue:41-46): `list` appends `list-container list-container--list` and stops (:42), every other value appends `list-container grid--{type}` (:45). `<div class=\"list-container__inner\">` is that div's only child (Grid.vue:3), and one `.grid__column` per item comes from GridItem.vue (GridItem.vue:2). src/scss/layout/_grid.scss:36-44 styles ONLY the descendant pair `.grid--threecol--33-34-33 .list-container__inner`, so the grid class by itself produces no grid and `list-container__inner` by itself produces nothing at all. THE PER-ITEM LEVEL IS THE ONE THAT GETS DROPPED, AND IT DOES TWO JOBS THAT ARE ROUTINELY CONFLATED — one is class-agnostic and the other is not. (a) ROW SPACING IGNORES THE CLASS ENTIRELY: the rule is `.list-container__inner > div + div` (src/scss/layout/_grid.scss:103-109), which keys on DIRECT DIV CHILDREN, so any div gets the 2rem whatever it is called. That is exactly why ONE rule spaces `.grid__column` in UIDS and `.views-row` downstream, and why the class triple's third slot differs between the two views without the spacing changing. (b) THE CARD CONTAINER CONTEXT KEYS ON THE CLASS: `.grid__column` is the only PER-ITEM selector UIDS gives `container-name: column` (_grid.scss:112-119 — the other two, `.column-container` and `.layout-container`, are region wrappers), and without it card's 400px container query never fires, which makes the `orientation` pin inert. The two failure modes are independent, and the pair is why a hand-rolled row `<div>` looks nearly right: it satisfies (a) and fails (b), i.e. correctly spaced and silently mis-laid-out. See behavior. DOWNSTREAM THE SAME THREE LEVELS COME FROM DRUPAL, not from Grid.vue: views-view.html.twig:40 puts `list-container` on the view wrapper and :75 puts `view-content list-container__inner` on the rows wrapper, uiowa_core.module:1749 adds `list-container--list` to every view not on its exclusion list, and each row is a `.views-row`, which global.scss:19-22 gives the column container in place of `.grid__column`. TWO CLASSES DELIBERATELY LEFT OUT of the triple: `list-container--grid` is required only on the Layout Builder / inner-grid path (_grid-mixins.scss:20-57, contracts/layout.json contentGrid) and in the Claude Design view (claude-design/layout.css:116-123), not by _grid.scss's own standalone rules, so a page that omits it there renders stacked; and contracts/layout.json owns the column-ratio vocabulary, which this pattern does not re-declare. Card.stories.js:192-193 is worth reading as the counter-example — it wraps a single card in `grid--threecol--33-34-33 > .grid__column.list-container`, which never forms the descendant pair and therefore lays out nothing; that wrapper exists purely to supply `.grid__column`'s container. BOTH ARRANGEMENTS ARE NOW DRAWN IN STORYBOOK off the real Grid/GridItem pair (GridTemplate, Card.stories.js:345-382): the `Grid` story (:384-390) is the grid counter-case, and the `ItemList` story (:404-417, story id `components-card--item-list`, added 2026-08-21 — see changes) is the list, with children.card.fixed.list as its args."
      }
    }
  ],
  "slots": [
    {
      "name": "title",
      "description": "The item's name, and — whenever the row has a url — its anchor. Mechanics belong to contracts/card.json slots.title; the pattern only pins that the item's name goes HERE rather than into body copy.",
      "behavior": "That placement is load-bearing, not stylistic: a url with a title present is what makes the title the real link and degrades the footer to the non-interactive arrow indicator (Card.vue:141-159). Putting the name in the body would move the anchor onto the button and change every row's markup."
    },
    {
      "name": "subtitle",
      "description": "WHERE THE DATE GOES, and it is not a per-page choice: sitenow_articles Article.php:88-92 sets `#subtitle` to the node's created date formatted `medium`, unconditionally unless `created` is in `#hide_fields`. EVERY article card therefore carries its date in `card__subtitle` (uids_base templates/uids/card.html.twig:38-44). CORRECTED 2026-08-22 — an earlier draft paired that with \"and uses `card__meta` on exactly one\", under a heading reading WHERE THE DATE GOES, which invites the reading that the date sometimes lands in `card__meta` instead. It never does. `card__meta` is a SIBLING of `card__subtitle` inside `card__details` (card.html.twig:36-53; the two arms are independently optional), and for an Article `#meta` holds `field_article_author` (Article.php:37-41) and/or a `field--article-byline` span built from `field_article_source_org` / `field_article_source_link` (Article.php:66-85). \"Exactly one\" was a count of how many of the sandbox's ten articles happen to carry an author or a source — content, expected to drift, and never the date.",
      "behavior": "Pinned because the obvious guess is `meta` — the repo's own eval fixture guesses meta, see knownIssues.governed-fixture-diverges — and the two slots are visibly different: subtitle is Zilla Slab at card/subtitle/* with opacity 0.7, meta is Roboto 400 at card/meta/*. Both are card's (contracts/card.json slots.subtitle, slots.meta)."
    },
    {
      "name": "media",
      "description": "Optional PER ITEM. Framed by the media_size / media_shape pins in children.card.fixed, not by anything the list owns.",
      "behavior": "See behavior: the orientation pin is applied to every row whether or not that row has a picture, because card already gates the whole `.media` wrapper on slot presence (Card.vue:193)."
    }
  ],
  "tokensUsed": [],
  "children": [
    {
      "component": "card",
      "contract": "contracts/card.json",
      "inherits": [],
      "fixed": {
        "list": {
          "borderless": true,
          "orientation": "right",
          "link_text": "",
          "link_indicator": true,
          "media_size": "small",
          "media_shape": "widescreen",
          "headline_style": "serif"
        },
        "grid": {
          "borderless": false,
          "orientation": "",
          "button_align_bottom": true,
          "headline_style": "serif"
        },
        "notes": {
          "borderless": "`true` for list. The design system owner's first pin — border on/off (recorded in the research fork, .claude/plans/2026-08-21_figma-punch-list-triage.md:152-157) — and named by contracts/rules.json item-list-is-stacked-cards; sourced rather than observed (2026-08-22): SiteNow makes borderless the default for EVERY node rendered as a card (NodeBundleBase.php:99, `'border' => 'borderless'`), RendersAsCardTrait.php:35-46 pushes each default style straight onto the card's `#attributes['class']`, and no `#override_styles` touches this view — the only article-side override is scoped to related_manually_referenced_content/block_grid (sitenow_articles.module:413-431). CORRECTED: an earlier draft called this \"confirmed twice in the shipped system\", counting the reference page's markup and NodeBundleBase as two independent confirmations. They are not independent — the markup is CAUSED by the default, so reading the page can only ever corroborate the thing it was rendered from. One chain, traced end to end, is what this is. Note also that `card--layout-right borderless click-container` is a SUBSET of the wrapper's classes, not the whole attribute: card.html.twig:1 adds `card` and uiowa_core.module:537 adds `block--word-break`, while the `media--*` and `headline*` styles are moved off to media_attributes and headline_class before render (uiowa_core.module:469-475). It is not only the outline. With no background class the borderless block also zeroes the card's 2rem padding and the media__inner offsets (card.scss:294-310), so each row sits flush in its column and the stack reads as one sequence instead of ten boxes stacked edge to edge. `false` for grid, which is card's own default: the outline is what makes a small set of parallel destinations read as separate tiles rather than as a feed.",
          "orientation": "`\"right\"` for list — MEDIA ON THE RIGHT, the design system owner's second pin (image position) and the one most likely to be got wrong. Every reference row carries `card--layout-right` — sourced, not counted: it is NodeBundleBase.php:96 reaching the wrapper via RendersAsCardTrait.php:35-46 with no override on this view (see the borderless note on why a reading of the page is not a second confirmation). Whichever rows DO have an image emit `.media--widescreen.media--small.media` FIRST in the DOM and `card--layout-right`'s `flex-direction: row-reverse` (card.scss:468-477) moves it to the right of the copy. SiteNow sets it as the base default for every card-rendered node (NodeBundleBase.php:96); only Person (sitenow_people Person.php:97), Event (uiowa_core Event.php:58, sitenow_events Event.php:57), ThesisDefense (:37) and GradStudentProfile (:36, :43) override to `card--layout-left`, and Article — the news bundle — does not, so a news listing is right-oriented. Borderless AND oriented is a combination the SCSS handles deliberately rather than by accident: card.scss:311-326 re-adds the single card/layout/media-gap gutter that the borderless padding reset had just removed, and Card.stories.js:299-312 (PersonProfile) is the same combination in a single card, with the `ItemList` story (:404-417) now showing it repeated down a list. `\"\"` for grid, and that is the honest value rather than a shrug: below the 400px container threshold a narrow grid column renders all three orientations identically (contracts/card.json options.orientation), so pinning a side there would assert a difference the CSS cannot produce. contracts/card.json owns the mechanics. Read knownIssues.governed-fixture-diverges before copying the eval fixture, which sets \"left\".",
          "link_text": "`\"\"` — the empty string — for list. This is \"the arrow button\", the design system owner's third pin. Card selects its button treatment on the PRESENCE of link_text rather than on any style prop: empty, with a url and link_indicator on, pushes `bttn--circle` + `bttn--no-text` (Card.vue:126-129) and renders the circular arrow-only indicator that contracts/card.json options.link_text describes and its Figma map calls Circle. Downstream the same classes are assembled in PHP: the base four at uiowa_core.module:539-544, with `bttn--circle` + `bttn--no-text` appended at :553-559 when a url is set, `#link_text` is NOT SET, and `#link_indicator` is truthy — so the reference page emits `bttn bttn--primary bttn--transparent bttn--light-font bttn--circle bttn--no-text` wrapping `<i role=\"presentation\" class=\"fas fa-arrow-right\">` on every row that reaches the footer at all. TWO CORRECTIONS FROM READING THAT SOURCE (2026-08-22), both about how much the reference page can be asked to prove. (a) THE INDICATOR IS CONFIG-GATED AND SHIPS OFF. `#link_indicator` is not intrinsic to the bundle — it is read from `sitenow_articles.settings:show_teaser_link_indicator` (NodeBundleBase.php:60-65), whose shipped value is FALSE in both config/default/sitenow_articles.settings.yml:2 and the module's own config/install copy, and which is a per-site toggle on sitenow_articles/src/Form/SettingsForm.php:312. card.html.twig:61 gates the ENTIRE `<footer>` on `url is not empty and (link_text is not empty or link_indicator)`, and Drupal never sets `#link_text` for an Article — so under DEFAULT configuration a SiteNow news listing renders no button at all, circle or otherwise. The arrow on the sandbox page is evidence that THAT SITE has the toggle on, which this repo cannot read. The PIN is untouched by this: it is the design system owner's ruling about what a list should render, not a report of what SiteNow defaults to. What changes is the weight of the citation — do not quote the reference page as proof that the shipped system agrees. (b) THE TWO VIEWS TEST DIFFERENT THINGS, and only one of them accepts the pinned value literally. Card.vue:126 tests FALSINESS (`!props.link_text`), so `\"\"` produces the circle in UIDS. uiowa_core.module:553 tests ABSENCE (`!isset($element['#link_text'])`), so an empty STRING passed there would be `isset` and would SUPPRESS the circle. Nothing is broken today, because Drupal leaves the key unset for Articles (only uiowa_core Card.php:44 and TimelineItem.php:56 ever set it) — but `link_text: \"\"` is a UIDS-shaped expression of the pin, and the downstream expression of the same pin is \"do not set it\". A labelled \"Read more\" repeated down a feed is what this pin exists to prevent. DELIBERATELY ABSENT from fixed.grid: the grid case needs a non-empty LABEL and the label text is content, so there is no value to pin — Storybook's Grid story inherits Default.args's \"Explore the arts\" (Card.stories.js:233, 384-390), while the ItemList story overrides it to `''` (:415).",
          "link_indicator": "`true` for list. Card's own default (contracts/card.json options.link_indicator), pinned EXPLICITLY because it is the other half of the condition above and its false branch fails quietly: `link_indicator: false` with no link_text renders no footer at all (Card.vue:243), so the arrow simply is not there and nothing complains. Absent from fixed.grid because it has no effect there — once link_text is non-empty, Card.vue:126's `!props.link_text` guard is false and Card.vue:243's footer renders on link_text alone.",
          "url": "NOT A FIXED VALUE, so it is not in either map — but every item MUST carry a non-empty one. It is required content, and it also selects the row's whole markup shape; behavior holds the mechanism and the evidence.",
          "media_size": "`\"small\"` for list. Not one of the design system owner's three, and not cosmetic: card's OWN default is `large` (contracts/card.json options.media_size), and inside an orientation block `media--large` takes `flex: 0 0 52.5%` (card/layout/media-basis/large, card.scss:445-449) — over half the row. An item list that leaves this unpinned is a stack of half-image banners, not a feed. `media--small` caps a widescreen thumbnail at 15.625rem (card.scss:426-429, itself deliberately un-tokenized — see contracts/card.json geometryNotTokenized.side-by-side-media-caps). Source: NodeBundleBase.php:98, matching the reference page's `media--widescreen media--small media`. Not pinned for grid: with no orientation the whole card.scss:395-449 side-by-side block is out of play, so media_size stops being a layout decision and goes back to the author.",
          "media_shape": "`\"widescreen\"` for list. NodeBundleBase.php:97 (`'media_format' => 'media--widescreen'`) and the reference page agree. The foundation contracts/media-framing.json owns the option; this records only which value the shipped list uses. Left to the author for grid, where nothing about the arrangement forces a crop.",
          "headline_style": "`\"serif\"` in BOTH arrangements — the one pin that does not invert. Every reference row renders `headline headline--serif headline--default`, and the chain is: SiteNow sets `card_headline_style => 'headline--serif'` (NodeBundleBase.php:95), uiowa_core.module:472-475 moves any `headline*` style off the wrapper into `headline_class`, and headline.html.twig:5-6 supplies `headline--default` as the default `headline_location` — so all three classes are structural, not authored per row; and every authored Card story sets it (contracts/card.json options.headline_style). Read contracts/card.json knownIssues.headline-style-divergence first: production does not consume card's `headline_style` prop at all, it reaches serif through headline_class in the downstream twig. The pin is therefore on the RESULT, not on a prop name only UIDS honours.",
          "button_align_bottom": "`true` for grid ONLY, and it is the one entry here that is a PRESCRIPTION rather than an observation: Storybook's Grid story leaves card's default `false` (Card.stories.js:235, 384-390) and no shipped grid page was read back for it. It is prescribed because the option exists ONLY to line buttons up across a row of unequal-height siblings (contracts/card.json options.button_align_bottom), which is exactly the grid case — and correspondingly means nothing in a one-per-row list, which is why it is absent from fixed.list. Note it is also the only pin that rides a 768px VIEWPORT query rather than card's 400px container query (card.scss:335-354)."
        }
      }
    }
  ],
  "behavior": [
    "EVERY ROW MUST ESTABLISH THE `column` CONTAINER, or the orientation pin is INERT — and it fails silently, rendering a plausible-looking stacked card rather than an error. Card's layout switches on `@container column (min-width: 400px)`, not on the viewport (contracts/card.json behavior). Each view supplies that context somewhere different: UIDS declares it on `.grid__column`, `.column-container` and `.layout-container` (src/scss/layout/_grid.scss:112-119), which is why the per-item level is written into options.arrangement.code.classes rather than left implicit; SiteNow declares it on the list's own rows, `.list-container__inner .views-row { container-type: inline-size; container-name: column }` (uids_base scss/global.scss:19-22); the Claude Design view ships a `.dc-card-column` wrapper for the same reason (claude-design/readme.md:166-170, Card.dc.html:41). A hand-rolled row wrapper that declares none of them gets no container, no 400px query and no orientation. DO NOT MISTAKE ROW SPACING FOR EVIDENCE THAT THE CONTAINER IS THERE: the 2rem gap comes from `.list-container__inner > div + div` (_grid.scss:103-109), which never looks at the class, so a plain `<div>` row is spaced exactly like a `.grid__column` and laid out nothing like one. Correct rhythm with a stacked card is the signature of this failure, not a refutation of it. This is the part of the pattern no checker can catch.",
    "THREE OF THE PINS ARE A RULING, THE REST ARE THE SHIPPED RECIPE, and the difference matters to whoever revisits this. The design system owner pinned border on/off, image position and the arrow button (punch-list triage 2026-08-21:152-157, TASK D) — children.card.fixed.list's `borderless`, `orientation` and `link_text`. `link_indicator`, `media_size`, `media_shape` and `headline_style` are not rulings: they are what SiteNow's defaults and the reference page actually emit, recorded so that following the pattern reproduces the page instead of approximating it. `button_align_bottom` is neither — it is the one prescribed value, argued from what the option is for rather than from anything read back; fixed.notes says so at the entry. Any card option NOT listed for an arrangement keeps card's own default and stays the author's to set. Since 2026-08-21 the list half of that map is also executable: Storybook's `ItemList` story (Card.stories.js:404-417) sets exactly children.card.fixed.list. Nothing BINDS the two — no checker compares a story's args to a contract's fixed map — so treat the contract as the source and the story as the reproduction, and change both together.",
    "THE GRID COUNTER-CASE INVERTS THE PINS, which is the practical way to tell the two apart rather than arguing about the words \"list\" and \"grid\". The inversion is DATA, not prose — compare children.card.fixed.list with children.card.fixed.grid — and it runs in both directions: a value pinned for one arrangement is often deliberately absent from the other, because the arrangement makes it meaningless (link_indicator and media_size in a grid, button_align_bottom in a one-per-row list) rather than because nobody got to it. The arrangements also differ in MARKUP, not only in card options: options.arrangement.code.classes carries a different class triple for each, and swapping the pins without swapping the wrapper produces bordered cards in a single stacked column.",
    "EVERY ITEM CARRIES A NON-EMPTY `url`, and it is not in children.card.fixed because it is required content rather than a fixed value. It is what makes a row a destination, and it also selects the row's whole markup shape: a url WITH a title makes the TITLE the anchor and degrades the footer to a non-interactive PseudoButton `<div>` (Card.vue:141-159, :243-247), and it adds `click-container` so the entire row is clickable (Card.vue:108-110). The reference page shows exactly that — `<a class=\"click-target\">` inside `<h2 class=\"headline headline--serif\">`, and a `<div>` footer. Its absence takes the arrow pin down with it: Card.vue:243 gates the whole footer on `url && (link_indicator || link_text)`, so a url-less row renders no button at all whatever link_indicator says.",
    "MEDIA IS PER ITEM, NOT PER LIST — and as of 2026-08-22 it is not the only per-item variation. Media comes from `field_image` on the node (NodeBundleBase.php:51), so whether a given row has one is CONTENT: the \"nine of ten body-only, one with an image\" split this entry used to state as fact is a snapshot of the sandbox's articles, expected to drift, and nothing to design against. The pins are applied uniformly regardless — `card--layout-right` sits on every row — because card already gates the whole `.media` wrapper on slot presence (Card.vue:193, and card.html.twig:4-18 downstream). Do not branch the pattern on whether an individual item has a picture; that produces two visually inconsistent row types for no reason. THE SECOND VARIATION IS NOT A PICTURE: NodeBundleBase.php:76-85 emits a `#pre_title` \"Pinned\" thumbtack — rendered as `card__pre-title` (card.html.twig:22-26) — for a sticky node whenever the view carries a sticky sort, and page_articles declares exactly that sort (views.view.articles.yml, its own `sorts: sticky`, order DESC, one of the two things it does not inherit). So the ten rows are not guaranteed structurally uniform even before content differences, and UIDS has no equivalent slot — Card.vue offers no pre-title, so a UIDS-built list cannot reproduce a pinned row."
  ],
  "changes": [
    "2026-08-21 — GRID LEARNED A `list` TYPE AND UIDS TOOK OWNERSHIP OF THE LIST ROW SPACING. Two files carry the change, and a third exercises it. Grid.vue now branches on the type: `list` emits `list-container list-container--list` and appends NO `grid--*` class, every other value still emits `list-container grid--{type}` (Grid.vue:41-46). src/scss/layout/_grid.scss:103-109 now carries a VERBATIM mirror of the row-gap rule that until this date lived only in uiowa/docroot/themes/custom/uids_base/scss/components/grid.scss:6-12. Card.stories.js:404-417 adds the `ItemList` story (`components-card--item-list`) that renders the result. THIS ENTRY REPLACES A KNOWNISSUE, deliberately and not silently: the file previously carried `grid-vue-cannot-emit-the-list-wrapper`, which is now false in its own title. It is filed here rather than left there because knownIssues records DEFECTS and this defect is closed, and because a resolved entry in a defect list is worse than no entry — a reader either acts on a claim that no longer holds or 'fixes' something already fixed. The finding that entry carried is therefore reproduced in full below; nothing about it is lost by the move. WHAT WAS TRUE UNTIL THIS DATE: `<uids-grid>` appended a `grid--{type}` class unconditionally (default `threecol--33-34-33`), UIDS contained the string `list-container--list` nowhere, and the class reached a page only downstream, added by uiowa_core.module:1749 on Drupal views. The non-obvious half — the reason this was not merely a missing modifier — is that the row-gap rule's `:not([class*=\"grid-\"])` guard excludes EVERY `grid--` value INCLUDING `grid--onecol`, because the substring `grid-` is present in the name. So the nearest available approximation, `type=\"onecol\"` (stacks the columns and caps the inner width at 81.875em, _grid.scss:4-9), could never receive the gap, and _grid.scss gives a gap only to the multi-column ratios (:26-76). The design system consequently MANDATED an arrangement — contracts/rules.json `item-list-is-stacked-cards` — that the design system itself could not render: no story, no regression baseline, no Claude Design example, only a live Drupal page. That is the reason for the change, and it is also why `list` is a SIBLING of the column ratios and not an alias for `onecol`: a one-column grid and a list are different arrangements, and only the latter gets the 2rem. DOWNSTREAM IS UNAFFECTED, checked rather than assumed. The mirror is identical in selector, declaration and value, so no SiteNow page moves a pixel; two identical rules resolve to one computed `margin-top`. A blast-radius grep of uiowa/uiowa for `list-container--list`, excluding vendor/ and node_modules/, returns TEN sites. SEVEN only ADD the class: uiowa_core.module:1749, uiowa_maui.module:13, uids_base/templates/paragraphs/paragraph--events--default.html.twig:48, config/default/layout_builder_styles.style.list_format_list.yml:7, admissions_core.module:585, and housing_core.module:45 and :177. TWO READ it: LayoutBuilderStylesHelper.php:183 is the positive test, and layout_builder_custom.module:1303 is an ABSENCE test — `if (!in_array('list-container--list', $style_map))` appends GRID_PREFIX, i.e. 'no list class means treat it as a grid'. Exactly ONE styles it: uids_base/scss/components/grid.scss:6, the file now mirrored. An earlier draft of this entry said 'nothing keys off the class's absence'; that was FALSE — :1303 does. The change is still safe, for a different and stronger reason: both readers run in PHP over a Drupal style map and never observe UIDS markup, and `grep -rn uids-grid` over uiowa/docroot returns nothing, so no downstream render path passes through Grid.vue. The Vue branch is additive because it is unreachable from Drupal, not because the absence goes unobserved. VERIFIED IN A LIVE BROWSER rather than inferred: the ItemList story's outer div is `class=\"list-container list-container--list\"` with no grid class, and `.list-container__inner`'s three `div.grid__column` children compute `margin-top` 0px / 32px / 32px with `container-name: column` on all three — i.e. both jobs of the per-item level (see options.arrangement.code.notes) are satisfied at once. WHO AUTHORISED IT, because the retired knownIssue named a ruling as the blocker and that reversal must be on the record rather than inferred: its closing clause read 'Recorded rather than fixed — adding a `--list` modifier to Grid.vue is a component change, and the component set is closed.' the design system owner lifted that on 2026-08-21, asking directly for the pattern to 'make sense in UIDS and as something that can be shown in Figma as well'. The earlier entry had also over-read the ruling: 'the component set is CLOSED' was a scope decision about MIGRATING NEW components after banner and card, and Grid.vue was already in the repo — so teaching an existing component a new value for an existing prop was never the thing it prohibited. Note the asymmetry with knownIssues `grid-has-no-contract-and-no-barrel-export`, which still cites the same ruling to refuse minting a grid contract: that one stands, because a contract for a component that has never had one IS a scope expansion. Do not fold the branch back into a single template string, and do not delete the SCSS mirror as a duplicate of the theme's: UIDS owning its own copy IS the change."
  ],
  "knownIssues": [
    "grid-has-no-contract-and-no-barrel-export: the `list` value this pattern now leans on is public shape with no governance behind it, and that gap is the open question this contract cannot close for itself. There is no contracts/grid.json — Grid.vue and GridItem.vue appear in identity.relatedPaths but are claimed by no contract's identity.codePath — so the `type` prop's value set, its default and the `list` branch are validated by nothing: check-contracts.mjs parses Vue props only for contracts that declare a codePath. composedOf stays `[\"card\"]` for the same reason, since check-contracts.mjs:86-87 rejects a composedOf id that is not a contract in this repo; do not add \"grid\" there expecting it to mean 'a grid contract should exist'. Neither component is in the barrel either — src/components/index.ts (14 exports) has no Grid entry and src/components/grid/ has no index.ts — so `<uids-grid>` is reachable only by direct path import, which is what Card.stories.js:2-3 does. Recorded here rather than acted on: the contract schema has no openQuestions field, so knownIssues is where an unresolved gap goes, and minting a grid contract would be a decision against the closed-component-set ruling, not a pattern contract's call. Nothing in this contract should be read as implying a grid contract exists; where it needs the column-ratio vocabulary it points at contracts/layout.json, and where it needs Grid's markup it cites the .vue files by line.",
    "circle-indicator-unnamed-in-uids: the two implementations of this row disagree on accessibility, and as of 2026-08-22 the Drupal half is confirmed FROM SOURCE rather than from a reading of the page. uiowa_core.module:513-516 sets `aria-hidden=\"true\"` on the button whenever `linked_element !== 'button'` — which for an article is always, since a url plus a title resolves `linked_element` to `'title'` (:483-485) — and card.html.twig:67 and :73 hardcode `role=\"presentation\"` on the `<i class=\"fas fa-arrow-right\">` in BOTH the anchor and the pseudo-button arms, unconditionally. The emitted markup is therefore `<div aria-hidden=\"true\" class=\"... bttn--circle bttn--no-text\"><i role=\"presentation\" class=\"fas fa-arrow-right\">`, hiding the decorative indicator from assistive tech because the title already carries the link (`<a class=\"click-target\">` inside the `<h2>`, uiowa_core.module:506-510 + headline.html.twig:8). UIDS emits neither attribute — `aria-hidden` and `role` appear nowhere in Card.vue or src/components/button/PseudoButton.vue (note the path: there is no src/components/pseudo-button/), verified by grep. A UIDS-built list therefore exposes one unnamed control per row where the Drupal one exposes none. The underlying gap is already recorded in contracts/card.json a11y and contracts/button.json; it is repeated here only because a list multiplies it by the row count, and the fix belongs upstream in Card/PseudoButton, not in this pattern."
  ],
  "geometryNotTokenized": {
    "row-gap": "The vertical space between rows belongs to the LIST container, not to the card, and this pattern deliberately does not claim a token for it. THE SHIPPED VALUE IS 2rem: `.list-container.list-container--list:not([class*=\"grid-\"]) .list-container__inner > div + div { margin-top: 2rem }`. AS OF 2026-08-21 BOTH SIDES CARRY THAT RULE — UIDS at src/scss/layout/_grid.scss:103-109 and uids_base at uiowa/docroot/themes/custom/uids_base/scss/components/grid.scss:6-12 — identical in selector, declaration and value, both read from the files. Nothing double-applies and nothing shifts downstream: two identical rules resolve to one computed `margin-top`, whichever wins. uids_base's copy is now a REDUNDANT MIRROR — its own file already `@use`s the UIDS layout partial (grid.scss:3), so once the theme picks up a UIDS release containing this rule the stylesheet simply emits it twice — and deleting it there is a tidy-up for whoever owns that theme, not a prerequisite for anything here. See changes for why UIDS took the rule on. The selector is what matches the reference page's `list-container list-container--list` wrapper — views-view.html.twig:40 supplies `list-container`, uiowa_core.module:1749 supplies `list-container--list`, and neither adds a `grid--*` class, so the `:not()` guard passes; Grid.vue:41-43 now produces the same grid-less container in UIDS. (An earlier draft credited uids_base scss/paragraphs/uiowa-paragraphs-lists.scss. It cannot apply: every `.views-row` rule in that file sits under a `.paragraph--type--people` / `--articles` / `--events` / `--featured-content` ancestor — :289, :335, :416, :459 and the :512-586 blocks — and the reference page emits no `paragraph--type--*` class at all.) STILL DELIBERATELY NOT TOKENIZED, and now that UIDS owns the rule that is a decision rather than a consequence of not owning it. The 2rem is not a new magnitude: `$mobile-height-gutter` IS 2rem (src/scss/abstracts/_variables.scss:81), already tokenized as layout/gutter/height/mobile = {space.200} (tokens/semantic/layout.json:9) and claimed by contracts/layout.json under the one-contract-claims-each-token rule. Tokenizing the row gap would therefore mint a tier-3 token AND a Figma variable for a value the system already has, to serve one declaration in a pattern that has no Figma component set to bind it to. UIDS's own containers also still disagree about gaps — the multi-column ratios set `gap: variables.$mobile-height-gutter` (src/scss/layout/_grid.scss:26-76) while `grid--onecol` sets none and only caps width (:4-9) — so anyone who wants this tokenized should tokenize the grid family as a whole rather than this one line. Whoever builds a list in a given view takes the gap from the rule above."
  },
  "a11y": {
    "notes": "A row's accessible surface is Card's derived-link model, and the pattern inherits both its strength and its gap (contracts/card.json a11y). The strength: exactly one real anchor per row, on the title, with the footer arrow degraded to a non-interactive `<div>` so a click-container row never nests two focusable destinations — visible in the reference markup, where the `<a class=\"click-target\">` is inside the `<h2>` and the footer is a div. The gap is LOUDER IN A LIST than in a single card because it repeats once per row: the circle indicator is an icon-only control with no accessible name. See knownIssues.circle-indicator-unnamed-in-uids for how the two implementations differ on it."
  }
}
