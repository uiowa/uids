<div class='{{ slide_classes }}'>
  <div class="carousel-image">{{ 'an image would go here' }}</div>
  {% if slide_caption is not empty %}
    <div class="carousel-caption">{{ slide_caption }}</div>
  {% endif %}
</div>
