import {Meta} from '@storybook/blocks';
import { version } from '/package.json';

<Meta isTemplate title="Introduction" />

# University of Iowa Design System (UIDS)
<p>
<strong>UIDS</strong> is a design system to support building applications that support the University of Iowa brand. You can learn about the available components by browsing through them using the navigation on the left. You can read more at the <a href="https://brand.uiowa.edu/">Brand Manual website</a>.
</p>

# How to use

<p>To utilize a component from UIDS you will need to use the Base CSS styles listed below as well as the specific component CSS listed on each component Docs page.
HTML markup for each component can be found in the HTML tab on each component story.</p>

## Base CSS


- <a href={`https://github.com/uiowa/uids/tree/gh-pages/docs/v${version}/src/fonts.css`}>Fonts</a>
- <a href={`https://github.com/uiowa/uids/tree/gh-pages/docs/v${version}/src/base.css`}>Base</a>

