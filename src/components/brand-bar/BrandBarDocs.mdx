import {Meta, Title, Source, Primary, Controls, Stories} from '@storybook/blocks';
import {version} from '/package.json';

<Meta isTemplate />

<Title />

## Usage
Make sure you are familiar with the [**Getting Started**](../?path=/docs/introduction#getting-started) guide first.

Add the following files to your project:
- <strong><a href={`https://github.com/uiowa/uids/tree/gh-pages/docs/v${version}/dist/css/components/logo.css`}>logo.css</a></strong>
- <strong><a href={`https://github.com/uiowa/uids/tree/gh-pages/docs/v${version}/dist/css/components/brand-bar.css`}>brand-bar.css</a></strong>

Refer to the HTML tab for example HTML.

<Primary withSource="none" />

## Inputs

The component accepts the following inputs (props):

<Controls />

---

## Additional variations

Listed below are additional variations of the component.

<Stories />

