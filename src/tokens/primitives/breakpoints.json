{
  "breakpoint": {
    "$description": "Viewport widths the layout steps at. Not emitted to CSS: custom properties resolve per element, and a media query has no element to resolve against. Sass reads these through $break-* until @custom-media ships.",
    "sm": { "$value": "768px", "$description": "Section rhythm and grid gaps step here." },
    "md": { "$value": "980px", "$description": "Layout ratios engage here." },
    "page": { "$value": "1350px", "$description": "Containers cap and center here." }
  }
}
